Master Prompt: Agentic AI Ecology Implementation
Role: You are a Senior Full-Stack AI Architect specializing in Python, Streamlit, and Agentic Workflows. You are an expert in deploying applications to Hugging Face Spaces and integrating Multimodal LLMs (specifically Google Gemini).
Context: I am providing you with a technical specification titled "System Summary & Entity Analysis (Date: 2026-01-27)". This document outlines a dual-modal system:
Interactive "WOW" Experience: A high-aesthetic document ingestion and agent orchestration tool.
Batch Automation Pipeline: A high-scale PDF trimming and summarization engine.
Source Material:
[PASTE THE FULL TECHNICAL SPECIFICATION TEXT HERE]
Task: Based on the provided specification and Entity Analysis, please perform the following implementation steps.
Phase 1: Architecture & Configuration
Project Structure: Generate a directory tree suitable for Hugging Face Spaces. It must distinguish between the interactive_app and batch_processor modules while sharing core utilities.
Configuration Files:
Create a template agents.yaml file defining a sample "Summarizer" and "Analyst" agent.
Create a template SKILL.md with global context instructions.
Create a requirements.txt file including streamlit, pypdf, google-generativeai, and any necessary UI helpers.
Phase 2: Core Logic Implementation (Python)
Please write the Python code for the following distinct modules. Use modular, class-based design.
The "WOW" Theme Engine:
Create a ThemeManager class.
Implement the logic to switch between standard Light/Dark modes and the "Painter Styles" (e.g., Van Gogh, Mondrian). Note: Use CSS injection in Streamlit to achieve these styles.
The "Coral" Formatter:
Write a function that takes raw text and converts it to Markdown.
Implement regex or string logic to identify key terms and wrap them in HTML/CSS spans with the color #FF7F50.
The Batch "Conveyor Belt":
Write a function process_directory_batch(path) that:
Scans for PDFs.
Uses pypdf to extract only Page 1 (Trimming).
Saves the trimmed file to a temporary directory.
Agent Orchestration:
Write a AgentChain class that reads from agents.yaml.
Implement a method execute_chain(input_text, agent_list) that passes the output of Agent A as the input to Agent B (Chained Execution).
Include a placeholder for the Gemini-2.5-flash API call.
Phase 3: The Main Interface (app.py)
Construct the main Streamlit application skeleton that:
Initializes Session State for file uploads and chat history.
Provides a sidebar for API Key Management (masked input) and Painter Style selection.
Implements tabs or a navigation bar to switch between the Interactive Workspace and the Batch Pipeline.
Interactive Tab: Showcases the file uploader and the "Coral formatted" output area.
Batch Tab: Provides a button to trigger the "Conveyor Belt" and displays the final "Table of Contents" (ToC).
Dashboard: Includes a placeholder metric component for "Token Consumption" and Agent Status (Pulse indicator).
Constraints & Guidelines:
Security: Ensure API keys are accessed via os.environ or user input, never hardcoded.
Performance: The batch process must be designed to run asynchronously or provide progress bars in Streamlit.
Model: Assume access to gemini-2.5-flash for the LLM calls.
UX: The code should prioritize the "WOW" factor—clean layout, clear headers, and use of Streamlit columns.
Output Format:
Please provide the code in code blocks separated by file name (e.g., utils/theme.py, app.py, config/agents.yaml). After the code, briefly explain how the "Coral Formatting" logic works.
